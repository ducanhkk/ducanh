#include <REGX51.H>
#define LED_PORT P2
sbit key = P3^3;
void delay_ms(unsigned int t)
{
	unsigned int x;
	for(x=0;x<t;x++)
		{;}
}
		unsigned char code code7seg[]={0xC0, 0xF9, 0xA4, 0xB0, 0x99, 0x92, 0x82, 0xF8, 0x80, 0x90};
void main()
{
	IT1=1;
  EX1=1;
  EA=1;
  }
void trinhNgat()interrupt 0	
{
	unsigned char dem;
	while(1)
	{
		if(key==0)
		{
			delay_ms(20);
			if (key==0);
			{
				while(key==0);
		dem++;
		if(dem>9){dem=0;}
		LED_PORT = code7seg[dem];
	}
}
}
}